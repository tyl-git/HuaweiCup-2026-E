# 问题一：6 条 P1 样本及新增静音异常复核工作表

日期：2026-09-24。以下六条均为 **pending**；本表整理自动证据和检查动作，不代替听音、看视频或逐词时间边界核验。请在本地 [Qt 展示程序](qt-showcase/2026-09-24_launch-multimodal-viewer_v1.0.bat) 的“选择片段”中选样本编号，播放、暂停并点击词条；需要逐帧时用左右方向键。人工复核必须使用本机 `q1-manual-review-normalized-v1.0/` 中的 `sample_XXXX__normalized.mp4`。这些副本已重置连续时间戳并通过全量 FFmpeg 审计；不要用 `mosei-stage-v1.0/` 判断播放器时长。原始材料、训练输入和特征文件保持不变。本机 `2026-09-24_q1-p1-review-contact_v1.0.png` 四帧联系图仅用于快速定位，未上传到精简包；[逐样本证据](2026-09-24_q1-p1-review-evidence_v1.0.json)记录各帧质量、数字音频和词时间。四张抽帧图不能证明整个视频有无面部，也不能认定发言人身份。

本轮进一步对 **100/100 个 WAV** 做全采样数字静音检查，发现 `sample_0042` 与原 P2 样本 `sample_0043` 两条整段为零采样；已分别从暂存视频直接解码并验证与 WAV 相同，且原音轨**不降采样、不合并声道**的浮点解码样本也全部为零，排除转 WAV 时消音的解释。新增 `0043` 的人工处置仍 pending，旧复核队列、原文、标签、NPZ 未被改写。证据 JSON 的 `all_audio_silence_scan` 留有 100 个 WAV 的路径、SHA-256、采样总数和非零数。

| 样本 / 媒体 | 原始转写与自动证据 | 优先人工检查 | 复核记录（待填） |
| --- | --- | --- | --- |
| `sample_0001` / `sample_0001.mp4`，5.399 s | “They've been able to find solutions or at least bring some **answers** to the table.” 15/15 词三模态有效。 | 听 3.8–4.6 s 并对照整句上下文；记下实际读词及 4.06–4.34 s 边界是否贴合声音。区分 `answers`、`matters` 或其他词，不凭 ASR 自动改原文。 | 实际听到：`matters`；整句：基本一致；边界：可用（大致对应）；说话人：可见；结论：原始转写词待修订，保留原始记录。 |
| `sample_0006` / `sample_0006.mp4`，3.916 s | 原文 `” [President Ronald Reagan:] "Look at the record`。`Look at the record` 的 4 词自动时间为 2.70–3.34 s；括号内容为说话人标注，不是口语。 | 听完整片段，确认标注性质并复核 2.70–3.34 s 四个口语词；检查说话开始时的人脸画面。 | 标注性质：元数据/字幕标注；四词时间：可用；说话人：可见；备注：开始说话时画面切到人物。 |
| `sample_0010` / `sample_0010.mp4`，5.659 s | “Structural mounts inside have to be properly stress analyzed so we make sure all that happens”。16 词都有强制对齐时间，但平均 CTC 概率仅 0.012，ASR 输出为空；OpenFace 142/142 帧 `success=0`、`confidence=0`，有效视觉词 0/16。 | 听完整片段并确认是否有人声、转写和画面说话人。 | 结论：没有人声，是带背景音乐的视频；原始转写不对应；词时间不可用；无可用说话人面部；状态：`unsupported_by_audio`。 |
| `sample_0011` / `sample_0011.mp4`，6.698 s | “So we coordinate with all the proper technical fields to get ready for the instrument to arrive here”。18 词有自动时间；ASR 却输出另一句，首词 `So` 被拉到 0.66–1.80 s；OpenFace 169/169 帧无有效视觉。 | 听完整片段，判断音轨是否对应原文，并复核前后段语音和画面。 | 结论：前半段为背景声、无对应人声；后半段有人声但不是给定转写；词语时间不可用；无可用说话人面部；状态：`transcript_mismatch`。 |
| `sample_0042` / `sample_0042.mp4`，6.054 s | “There are a dozen or more hands on techniques that we can do”。13 词形式上都有自动时间，但音轨经直接解码全部为 0，视觉也无有效帧。 | 核对原媒体音轨并检查画面；不要在静音上确认词边界。 | 结论：没有人声、背景声或音乐；无可见说话人；不适合词级对齐；原始转写未验证；状态：`unsupported_by_audio`。 |
| `sample_0088` / `sample_0088.mp4`，2.763 s | “going to get married or are already married”。8 词形式上都有自动时间，但 ASR 仅输出 “AA”，视觉无有效帧。 | 听完整短片并检查画面；不要在纯音乐上确认词边界。 | 结论：纯音乐，无人声；原始转写不对应；0.52–1.68 秒无对应语音；两个 `married` 均未听到；词语时间不可用；无可见说话人；状态：`unsupported_by_audio`。 |
| `sample_0043` / `sample_0043.mp4` | 原 P2 静音样本；音轨经数字检查全零。 | 听完整片段并检查画面。 | 结论：没有人声、背景声或音乐；画面有人出现但没有说话；不适合词级对齐；原始转写未验证；状态：`unsupported_by_audio`。 |

新增优先检查：**`sample_0043` / `sample_0043.mp4`**，原始身份 `-mJ2ud6oKI8/2`，原文 “So physical therapists and chiropractors will do joint manipulations and mobilizations”。该样本有 11 个自动词时间、7 个历史三模态 mask 通过词，但 WAV 整段为数字静音，暂存视频直接解码同样全零且逐采样吻合，不能支持这 11 个词的语音边界。OpenFace 的 171 帧中 111 帧 success=1、74 帧满足全部有效条件，说明**检测到脸和生成特征不能证明三模态语义正确对应**。请核对媒体与文本来源，记录可获得的补充证据；不得把自动词时间写成人工确认。人工处置：____；复核人：____；状态：pending。

## 客观检查结果与处理口径

- 四条全视觉缺失样本 `0010/0011/0042/0088` 的 OpenFace CSV 逐帧统计分别为 142、169、182、67 帧；四者每帧 `success=0`、`confidence=0`，因此按 `success=1` 且 `confidence>=0.8` 的门槛得到 0 个有效帧。**只能确定检测/跟踪未产出有效记录，不能仅凭这些值断言画面没有脸，也不能断言视频映射错误。** 即使以后看见脸，也须核实是不是实际发言者。
- 4 张等比例抽帧显示 `0010/0011` 为自然画面，`0042` 为器械产品画面，`0088` 为动画/图形画面；这是抽样观察，不扩展为全片结论。`0006` 的抽帧有字幕和历史影像/人物，显示为何仍需核验“检测到的人脸”和“正在发言的人”是否同一人。
- `sample_0006` 的 OpenFace 119 帧中 35 帧 `success=1`，仅 34 帧同时达到置信度门槛；这解释了视觉只覆盖 3/4 个有时间的口语词。四个无时间原词保留在词序列中，并用 false 掩码标记，不能解释为情感中性。
- 对 `0010/0011/0042/0088`，“所有词有时间”只表示 CTC 强制分配成功，不表示转写或时间可靠。尤其 `0042` 与新增 `0043` 是已证实的**数字静音**，合计 24 个词的现有时间缺乏语音支持，不能通过听音确认；须追查媒体/文本来源。当前汇总的 **1,923 个有时间且音频掩码为 true 的词仅为历史计算条件满足数，并不等于 1,923 个语音可靠词**。排除此两条的 24 个词后，为 **1,899 个候选有时间词**；历史三模态 mask 通过词 1,828 中还包含 `0043` 的 7 个词，排除数字静音后为 **1,821 个候选三模态词**。这两个候选数仍不是可靠边界数或准确率。其余 98 条非零音轨也不意味着都有可识别语音。`0010/0011/0088` 需先听音确认转写，随后才能决定是否重做词对齐；当前不要把三模态缺失掩码改为 true。
- 对 `0001`，若确认词不是 `answers`，需保留原始转写记录，另行形成经审核的处理副本，再重算该样本的 BERT 词特征和强制对齐时间，并检查其音视频词汇聚；只改展示字幕会使特征与文本不一致。对 `0006`，若说话人标注其实被念出，也须在处理副本恢复这些词的时间和三模态处理。**题目禁止更改样本情感标签，任何复核或文字处理都不得改标签，也不得覆写历史 NPZ。** 若四条视觉缺失片段能经合规方法提取并确认发言者面部，需要在新版重算对应视觉词特征与掩码，而不能填造 49 维数据。任何修订都应更新派生质检表并保留旧版供比对。
- 即使人工确认没有可用人脸，原始样本仍保留并记录视觉缺失；数字静音样本另记音频/时间质量异常，不把保存了音频特征文件等同于有可用语音。此结论应记录原因，不强行补视觉或填造语音边界。

核验依据：`2026-09-23_mosei_sample-manifest_v1.0.csv`、`2026-09-23_mosei-openface-qc_v1.0.csv`、逐帧 `mosei-openface-pilot-v1.0/*.csv`、`2026-09-24_mosei-word-alignment-check_v1.0.csv`、`mosei-word-times-v1.0/*.csv`、`2026-09-24_mosei-multimodal-check_v1.0.csv` 及最终 NPZ。上述文件均位于本目录。
